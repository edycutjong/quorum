# ── Quorum — Environment Variables ───────────────────────────
# This app runs 100% OFFLINE. No API keys required.
# All inference via @qvac/sdk (local Llama 3.2 1B).
# RAG embeddings via GTE-Large-FP16 on-device.
